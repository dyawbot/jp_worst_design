import 'package:flutter/material.dart';

class JpIOSSwitches extends StatelessWidget {
  const JpIOSSwitches({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
